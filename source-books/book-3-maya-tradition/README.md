# Maya tradition Telegram source archive

Primary source: the local Telegram Desktop export supplied for this task, copied byte-for-byte into `raw/` without altering the original export. `build_corpus.py` derives the JSONL records and conservative keyword index; `verify_corpus.py` checks their structural integrity.
